#include "main"
/**
 * main - entry point
 *
 * function that prints the alphabet, in lowercase
 * , followed by a new line.
 *
 * Return: always 0
 */

int main(void)
{
	_putchar(print_alphabet);
	_putchar('\n');
	return (0);
}
