#include "main.h"
/**
 * main - entry point
 *
 * function that prints the alphabet, in lowercase
 * , followed by a new line.
 *
 * Return: always 0
 */
void print_alphabet(void)
{
	char letter;
	return (0);
}
int main(void)
{
	print-alphabet;
	_putchar(letter);
	_putchar('\n');
	return (0);
}
