#include "main.h"
/**
 * _abt - absolute value for integer
 * @n: the int to check
 * Return: the absolute value
 */
int _abs(int)
{
	int n;

	if (n < 0)
	{
		_putchar(-n);
	}
	else
	{
		_puthar(n);
	}
}
