#include "main.h"

/**
 * *_strncpy - a function that copies a string
 * @src: appends the src string
 * @dest:  appends the src string to dest
 * @n: number of bytes
 * Return: dest
 */

char *_strncpy(char *dest, char *src, int n)
{
	int i = 0;
	char *m = dest;

	while (*m != '\0')
	{
		*m++;
  
